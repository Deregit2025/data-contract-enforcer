apiVersion: v3.0.0
id: week4-brownfield-cartographer
info:
  description: Auto-generated contract for Week 4 Brownfield Cartographer — Lineage
    Snapshots. Generated at 2026-04-03T07:59:45.798711+00:00.
  owner: week4
  title: Week 4 Brownfield Cartographer — Lineage Snapshots
  version: 1.0.0
kind: DataContract
lineage:
  downstream: []
  upstream: []
llm_annotations:
  codebase_root:
    business_rule: Must be a valid absolute file path string; should exist and be
      accessible; typically follows OS-specific path conventions (backslash for Windows,
      forward slash for Unix/Linux)
    cross_column_relationship: Associated with snapshot_id to identify which codebase
      version the snapshot represents; consistent codebase_root across snapshots indicates
      tracking of the same project over time
    description: The absolute file system path to the root directory of the codebase
      being analyzed for lineage tracking
  edges_confidence_max:
    business_rule: value must be a decimal between 0.0 and 1.0 inclusive, typically
      1.0 indicates full confidence
    cross_column_relationship: must be greater than or equal to edges_confidence_mean
      and edges_confidence_min; should align with captured_at timestamp for the same
      snapshot
    description: Maximum confidence score among all lineage edges in the snapshot,
      representing the highest certainty level of data lineage relationships captured
      at a point in time
  edges_confidence_mean:
    business_rule: value must be a decimal number between 0.0 and 1.0 inclusive; should
      be greater than or equal to edges_confidence_min and less than or equal to edges_confidence_max
    cross_column_relationship: 'must satisfy: edges_confidence_min <= edges_confidence_mean
      <= edges_confidence_max; should be null only if edges_count is 0 or null'
    description: The average confidence score across all lineage edges in the snapshot,
      representing the mean reliability or certainty of data lineage relationships
  edges_confidence_min:
    business_rule: value must be a decimal between 0.0 and 1.0 inclusive; must be
      less than or equal to edges_confidence_mean and edges_confidence_max
    cross_column_relationship: must be <= edges_confidence_mean and <= edges_confidence_max;
      should be null only if edges_count is 0 or null
    description: Minimum confidence score across all lineage edges in the snapshot,
      representing the lowest level of certainty in any data lineage relationship
      captured during this observation period
  edges_count:
    business_rule: value must be a non-negative integer; value should be >= 0 and
      typically correlates with nodes_count
    cross_column_relationship: should be consistent across snapshots with same git_commit;
      typically edges_count increases with nodes_count; edges_confidence_mean and
      edges_confidence_min are only meaningful when edges_count > 0
    description: The total count of edges (connections/relationships) in the data
      lineage graph snapshot for a given git commit
  git_commit:
    business_rule: value must be a 40-character hexadecimal string (SHA-1 hash format)
    cross_column_relationship: links to snapshot_id and codebase_root to identify
      the exact code version analyzed; multiple snapshots may share the same git_commit
      if analyzed differently
    description: Git commit SHA hash identifier that represents the version of the
      codebase at the time the lineage snapshot was captured
  nodes_count:
    business_rule: value must be a positive integer greater than 0
    cross_column_relationship: nodes_count must be greater than or equal to edges_count,
      as edges connect nodes and cannot exist without nodes
    description: The total count of nodes in the data lineage graph snapshot, where
      each node represents a data entity (e.g., table, view, dataset) in the codebase
      at a specific git commit
  snapshot_id:
    business_rule: Must be a valid UUID v4 format (8-4-4-4-12 hexadecimal characters);
      must be unique across all snapshots; cannot be null
    cross_column_relationship: Primary key for the snapshot record; paired with git_commit
      to link snapshot to specific code version and codebase_root to identify the
      repository being analyzed
    description: Unique identifier for each lineage snapshot capture, representing
      a specific point-in-time recording of the codebase lineage state
quality:
  specification:
    checks for week4-brownfield-cartographer:
    - row_count >= 1
    - missing_count(snapshot_id) = 0
    - duplicate_count(snapshot_id) = 0
    - missing_count(codebase_root) = 0
    - missing_count(git_commit) = 0
    - missing_count(nodes_count) = 0
    - missing_count(edges_count) = 0
    - missing_count(edges_confidence_mean) = 0
    - missing_count(edges_confidence_min) = 0
    - missing_count(edges_confidence_max) = 0
    - missing_count(captured_at) = 0
  type: SodaChecks
schema:
  captured_at:
    description: Column captured_at
    format: date-time
    required: true
    type: string
  codebase_root:
    business_rule: Must be a valid absolute file path string; should exist and be
      accessible; typically follows OS-specific path conventions (backslash for Windows,
      forward slash for Unix/Linux)
    description: The absolute file system path to the root directory of the codebase
      being analyzed for lineage tracking
    required: true
    type: string
  edges_confidence_max:
    business_rule: value must be a decimal between 0.0 and 1.0 inclusive, typically
      1.0 indicates full confidence
    description: Maximum confidence score among all lineage edges in the snapshot,
      representing the highest certainty level of data lineage relationships captured
      at a point in time
    maximum: 1.0
    minimum: 0.0
    required: true
    type: number
  edges_confidence_mean:
    business_rule: value must be a decimal number between 0.0 and 1.0 inclusive; should
      be greater than or equal to edges_confidence_min and less than or equal to edges_confidence_max
    description: The average confidence score across all lineage edges in the snapshot,
      representing the mean reliability or certainty of data lineage relationships
    maximum: 1.0
    minimum: 0.0
    required: true
    type: number
  edges_confidence_min:
    business_rule: value must be a decimal between 0.0 and 1.0 inclusive; must be
      less than or equal to edges_confidence_mean and edges_confidence_max
    description: Minimum confidence score across all lineage edges in the snapshot,
      representing the lowest level of certainty in any data lineage relationship
      captured during this observation period
    maximum: 1.0
    minimum: 0.0
    required: true
    type: number
  edges_count:
    business_rule: value must be a non-negative integer; value should be >= 0 and
      typically correlates with nodes_count
    description: The total count of edges (connections/relationships) in the data
      lineage graph snapshot for a given git commit
    maximum: 87.0
    minimum: 87.0
    required: true
    type: integer
  git_commit:
    business_rule: value must be a 40-character hexadecimal string (SHA-1 hash format)
    description: Git commit SHA hash identifier that represents the version of the
      codebase at the time the lineage snapshot was captured
    required: true
    type: string
  nodes_count:
    business_rule: value must be a positive integer greater than 0
    description: The total count of nodes in the data lineage graph snapshot, where
      each node represents a data entity (e.g., table, view, dataset) in the codebase
      at a specific git commit
    maximum: 51.0
    minimum: 51.0
    required: true
    type: integer
  snapshot_id:
    business_rule: Must be a valid UUID v4 format (8-4-4-4-12 hexadecimal characters);
      must be unique across all snapshots; cannot be null
    description: Unique identifier for each lineage snapshot capture, representing
      a specific point-in-time recording of the codebase lineage state
    format: uuid
    required: true
    type: string
servers:
  local:
    format: jsonl
    path: outputs/week4/lineage_snapshots.jsonl
    type: local
statistics:
  edges_confidence_max:
    flag: POSSIBLE_CLAMPED — mean > 0.99
    max: 1.0
    mean: 1.0
    min: 1.0
    p25: 1.0
    p50: 1.0
    p75: 1.0
    p95: 1.0
    p99: 1.0
    stddev: 0.0
  edges_confidence_mean:
    flag: POSSIBLE_CLAMPED — mean > 0.99
    max: 1.0
    mean: 1.0
    min: 1.0
    p25: 1.0
    p50: 1.0
    p75: 1.0
    p95: 1.0
    p99: 1.0
    stddev: 0.0
  edges_confidence_min:
    flag: POSSIBLE_CLAMPED — mean > 0.99
    max: 1.0
    mean: 1.0
    min: 1.0
    p25: 1.0
    p50: 1.0
    p75: 1.0
    p95: 1.0
    p99: 1.0
    stddev: 0.0
  edges_count:
    max: 87.0
    mean: 87.0
    min: 87.0
    p25: 87.0
    p50: 87.0
    p75: 87.0
    p95: 87.0
    p99: 87.0
    stddev: 0.0
  nodes_count:
    max: 51.0
    mean: 51.0
    min: 51.0
    p25: 51.0
    p50: 51.0
    p75: 51.0
    p95: 51.0
    p99: 51.0
    stddev: 0.0
terms:
  limitations: Do not modify field types without updating downstream consumers.
  usage: Internal inter-system data contract.
